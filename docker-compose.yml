version: "3.9"

services:
  mcp-manager:
    build: .
    container_name: mcp_manager
    ports:
      - "5001:5001"
      - "5002:5002"
      - "5003:5003"
    volumes:
      - /Users/Me:/mnt/host:rw   # full access to your home folder
    restart: unless-stopped
